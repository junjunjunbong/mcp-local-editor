{
  "name": "mcp-local-editor",
  "version": "0.2.0",
  "description": "A multi-workspace, session-confined MCP server for searching, reading, editing, testing, and reviewing local repositories.",
  "type": "module",
  "private": true,
  "bin": {
    "mcp-local-editor": "./src/cli.js"
  },
  "scripts": {
    "start": "node src/cli.js serve",
    "test": "node --test test/*.test.js",
    "check": "node --check src/cli.js && node --check src/core.js && node --check src/errors.js && node --check src/mcp-stdio.js && node --check src/process.js && node --check src/registry.js && node --check src/service.js && node --check src/sessions.js && node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "local-editor",
    "coding-agent",
    "workspace"
  ],
  "license": "UNLICENSED"
}
